# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SST Events' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SST Events

  pod 'Firebase'
  pod 'Firebase/Auth'
  pod 'GoogleSignIn'

  target 'SST EventsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SST EventsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
